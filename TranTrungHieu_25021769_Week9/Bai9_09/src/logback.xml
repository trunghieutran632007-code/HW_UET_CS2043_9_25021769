<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Mau format chung: thoi gian | thread | log level | logger | message -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 1) Ghi log ra console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 2) Ghi log ra tep vat ly: logs/app.log
         FileAppender don gian (ghi append vao 1 file).
         Neu can rolling theo ngay/dung luong, doi sang RollingFileAppender. -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file>
        <append>true</append>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Root logger: bat ca 2 appender, muc INFO tro len -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
