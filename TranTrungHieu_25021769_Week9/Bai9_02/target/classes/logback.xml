<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console: hiển thị log INFO trở lên cho người dùng cuối -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File: ghi đầy đủ DEBUG trở lên cho audit / debug -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/bank-system.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/bank-system.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger riêng cho code ứng dụng (default package): hiển thị từ DEBUG -->
    <logger name="Account" level="DEBUG"/>
    <logger name="Bank" level="DEBUG"/>
    <logger name="CheckingAccount" level="DEBUG"/>
    <logger name="SavingsAccount" level="DEBUG"/>
    <logger name="Transaction" level="INFO"/>
    <logger name="Main" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>
